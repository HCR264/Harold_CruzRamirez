version: '3.8'
services:
  db:
    image: postgres:18
    container_name: prueba_api_db
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

#  app:
#    build: .
#    container_name: prueba_api_app
#    ports:
#      - "8080:8080"
#    depends_on:
#      db:
#        condition: service_healthy

volumes:
  postgres_data: